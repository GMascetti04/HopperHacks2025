export interface Celebrity {
    name: string;
    image: string;
    context: string;
}

export interface Mood {
    id : string, 
    description : string, 
    emoji : string,
    message : string
}